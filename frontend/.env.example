# 前端环境模板：复制为 frontend/.env.local 后填写（所有 *.env.local 已被 gitignore）。
# 浏览器只访问同源 /v1；本地 pnpm dev 由 next.config.ts 代理到本机 API，生产由 Nginx 代理。
# 生产环境由 Docker/CI 注入（部署模式后端不读仓库 .env 文件，前端同样如此）。
NEXT_PUBLIC_API_BASE_URL=/v1
# 仅本地 `pnpm dev` 使用，改变后需重启开发服务器；不得使用 NEXT_PUBLIC_ 前缀。
# ORIONAMESH_API_DEV_UPSTREAM=http://127.0.0.1:8000
