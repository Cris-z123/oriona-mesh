# Specification Quality Checklist: 阶段 16 全局会话历史恢复

**Purpose**: 验证阶段 16 需求在进入实施前完整、可测试且与既有 MVP 边界一致。
**Created**: 2026-08-25
**Feature**: [spec.md](../spec.md)

## Content Quality

- [x] 阶段 16 聚焦“浏览全部本人历史”与“单会话单知识库回答”的边界分离。
- [x] 阶段 16 只改变会话列表 DTO 与查询语义；不改变 SSE、状态机、模型出口、会话绑定或 E2E 边界。
- [x] 受影响的用户故事、边界情况、功能需求、数据投影和 OpenAPI 均已更新。
- [x] 视觉、交互、状态职责和实施顺序分别只在 `ui-design.md`、`plan.md`、`tasks.md` 维护。

## Requirement Completeness

- [x] 无 `[NEEDS CLARIFICATION]` 标记。
- [x] 无筛选全局分页、可选知识库筛选、最近活动排序、知识库名称投影和租户隔离均具有可验证要求。
- [x] 用户故事覆盖未选择知识库浏览历史、跨知识库点击恢复和新建对话明确选库的连续路径。
- [x] 全局历史 total、缓存范围、当前选择和引用抽屉清理均具有明确边界。
- [x] 已明确更新公开会话列表契约，但不新增浏览器端 E2E。

## Feature Readiness

- [x] 所有阶段 16 功能需求都有对应验收场景或边界条件。
- [x] T170/T171 先覆盖失败行为；T172 冻结后端契约；T173 实现前端；T174 验证并交付。
- [x] 成功标准仍保持产品指标定位，不被转换为自动化量化门禁。

## Notes

- `knowledge_base_name` 是当前用户授权关联的响应投影，不是会话表的冗余列；用户隔离仍以每次查询的 `user_id` 边界保证。
- 侧栏默认不加知识库筛选控件，避免增加第二个范围选择器；保留 API 可选筛选以支持当前和未来受控调用。
